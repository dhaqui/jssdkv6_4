{
  "name": "paypal-v6-sdk-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server/node && npm install --include=dev && npm run build",
    "start": "NODE_ENV=production node server/node/dist/src/server.js"
  }
}
